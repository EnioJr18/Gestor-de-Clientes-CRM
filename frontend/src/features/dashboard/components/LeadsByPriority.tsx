import type { DashboardPriorityItem } from '../types/dashboard'
export function LeadsByPriority({ items }: { items: DashboardPriorityItem[] }) { const total = items.reduce((sum, item) => sum + item.count, 0); return <article className="lead-card"><h2 className="text-lg font-semibold text-strong">Prioridades</h2><p className="mt-1 text-sm text-muted">Distribuicao de prioridades dos seus leads.</p><ul className="mt-5 space-y-4">{items.map((item) => { const percent = total ? Math.round((item.count / total) * 100) : 0; return <li key={item.priority}><div className="flex justify-between text-sm"><span className="font-medium text-strong">{item.label}</span><span className="text-muted">{item.count} ({percent}%)</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-canvas"><div className="h-full rounded-full bg-brand" style={{ width: `${percent}%` }} /></div></li> })}</ul></article> }
