import { Bar } from 'react-chartjs-2'
import { BarElement, CategoryScale, Chart as ChartJS, Legend, LinearScale, Tooltip } from 'chart.js'
import type { DashboardEvolutionItem } from '../types/dashboard'
import { usePrefersReducedMotion } from '../../../lib/hooks/usePrefersReducedMotion'
ChartJS.register(CategoryScale, LinearScale, BarElement, Tooltip, Legend)
export function LeadsEvolutionChart({ items }: { items: DashboardEvolutionItem[] }) { const reduced = usePrefersReducedMotion(); const values = items.map((item) => item.count); return <article className="lead-card"><h2 className="text-lg font-semibold text-strong">Evolucao mensal</h2><p className="mt-1 text-sm text-muted">Leads criados em cada mes do periodo.</p><div className="mt-5 h-72"><Bar aria-label="Grafico de evolucao mensal" data={{ labels: items.map((item) => item.label), datasets: [{ label: 'Leads criados', data: values, backgroundColor: '#8b6cff', hoverBackgroundColor: '#a896ff', borderRadius: 8 }] }} options={{ responsive: true, maintainAspectRatio: false, animation: reduced ? false : { duration: 800, easing: 'easeOutQuart' }, scales: { y: { beginAtZero: true, ticks: { precision: 0, color: '#b5c0d7' }, grid: { color: '#2a3655' } }, x: { ticks: { color: '#b5c0d7' }, grid: { display: false } } }, plugins: { tooltip: { backgroundColor: '#121a30', titleColor: '#f2f5ff', bodyColor: '#f2f5ff' } } }} /></div><dl className="sr-only">{items.map((item) => <div key={item.month}><dt>{item.label}</dt><dd>{item.count} leads criados</dd></div>)}</dl></article> }
