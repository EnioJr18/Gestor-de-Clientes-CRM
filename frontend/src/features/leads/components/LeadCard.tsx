import { Pencil, Trash2 } from 'lucide-react'
import { Link } from 'react-router-dom'
import { LeadPriorityBadge } from './LeadPriorityBadge'
import { LeadStatusBadge } from './LeadStatusBadge'
import type { Lead } from '../types/lead'
import { formatLeadDate, leadFullName } from '../utils/leadFormatters'
export function LeadCard({ lead, onEdit, onDelete }: { lead: Lead; onEdit: (lead: Lead) => void; onDelete: (lead: Lead) => void }) { return <article className="lead-card"><div className="flex items-start justify-between gap-3"><div><Link className="font-semibold text-strong hover:text-brand" to={`/app/leads/${lead.id}`}>{leadFullName(lead)}</Link><p className="mt-1 break-all text-sm text-muted">{lead.email}</p></div><div className="flex gap-1"><button className="icon-button" type="button" aria-label={`Editar ${leadFullName(lead)}`} onClick={() => onEdit(lead)}><Pencil className="size-4" /></button><button className="icon-button text-danger" type="button" aria-label={`Excluir ${leadFullName(lead)}`} onClick={() => onDelete(lead)}><Trash2 className="size-4" /></button></div></div><p className="mt-3 text-sm text-muted">{lead.telefone || 'Telefone nao informado'}</p><div className="mt-4 flex flex-wrap gap-2"><LeadStatusBadge status={lead.status} /><LeadPriorityBadge prioridade={lead.prioridade} /></div><p className="mt-4 text-xs text-muted">Criado em {formatLeadDate(lead.criado_em)}</p></article> }
